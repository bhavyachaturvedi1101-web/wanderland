<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
  <div class="container-fluid px-4">
    <a class="navbar-brand d-flex align-items-center" href="/home">
      <i class="fa-solid fa-compass fa-2x me-2" style="color: #fe424d;"></i>
      <span class="fw-bold fs-4" style="color: #222222;">WanderLand</span>
    </a>
    <div class="collapse navbar-collapse pt-1 pt-sm-0" id="navbarNavAltMarkup">
      <div class="navbar-nav me-auto">
        <a class="nav-link fw-semibold px-3 py-2 mx-1 rounded-pill" href="/listings" style="color: #222222; transition: all 0.3s ease;">
          <i class="fa-solid fa-magnifying-glass me-1"></i>Explore
        </a>
        <a class="nav-link fw-semibold px-3 py-2 mx-1 rounded-pill" href="/listings/new" style="color: #222222; transition: all 0.3s ease;">
          <i class="fa-solid fa-plus me-1"></i>Add Listing
        </a>
        <% if(currentUser) { %>
          <button class="nav-link fw-semibold px-3 py-2 mx-1 rounded-pill d-lg-none" onclick="window.location.href='/favorites'" style="color: #222222; transition: all 0.3s ease;">
            <i class="fa-solid fa-heart me-1"></i>Favorites
          </button>
        <% } %>
        <div class="nav-item dropdown">
          <a class="nav-link dropdown-toggle fw-semibold px-3 py-2 mx-1 rounded-pill" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" style="color: #222222; transition: all 0.3s ease;">
            <i class="fa-solid fa-user me-1"></i>
            <% if(currentUser) { %>
              <%= currentUser.username %>
            <% } else { %>
              Account
            <% } %>
          </a>
          <ul class="dropdown-menu shadow border-0 rounded-3 mt-2" aria-labelledby="userDropdown">
            <% if(!currentUser) { %>
              <li><a class="dropdown-item py-2 px-3" href="/signup"><i class="fa-solid fa-user-plus me-2"></i>Sign Up</a></li>
              <li><a class="dropdown-item py-2 px-3" href="/login"><i class="fa-solid fa-sign-in-alt me-2"></i>Login</a></li>
            <% } else { %>
              <!-- <li><button class="dropdown-item py-2 px-3" onclick="window.location.href='/favorites'"><i class="fa-solid fa-heart me-2"></i>Favorites</button></li> -->
              <li><a class="dropdown-item py-2 px-3" href="/logout"><i class="fa-solid fa-sign-out-alt me-2"></i>Logout</a></li>
            <% } %>
          </ul>
        </div>
      </div>
    </div>
    <a class="nav-link fw-semibold px-3 py-2 mx-1 rounded-pill d-none d-lg-inline-block" href="/favorites" style="color: #222222; transition: all 0.3s ease;">
      <i class="fa-solid fa-heart me-1"></i>
    </a>
    <form class="search-container d-flex align-items-center ms-3 d-none d-lg-flex" action="/listings" method="GET">
      <input class="form-control search-input border-0 rounded-pill ps-3" type="search" name="search" placeholder="Search destinations..." aria-label="Search" style="border-color: #dee2e6; background-color: #f8f9fa;">
      <button class="btn search-toggle border-0 rounded-pill px-3 ms-1" type="button" style="color: #fe424d;">
        <i class="fa-solid fa-search"></i>
      </button>
    </form>

    <div class="dropdown d-lg-none">
      <button class="btn border-0 ms-0" type="button" id="searchDropdown" data-bs-toggle="dropdown" aria-expanded="false" style="color: #fe424d;">
        <i class="fa-solid fa-search fa-lg"></i>
      </button>
      <ul class="dropdown-menu dropdown-menu-center dropdown-menu-end shadow border-0 rounded-1 mt-4" aria-labelledby="searchDropdown">
        <li>
          <form class="d-flex align-items-center px-2 py-1" action="/listings" method="GET">
            <input class="form-control form-control-sm border-0 rounded-pill ps-2 me-2" type="search" name="search" placeholder="Search destinations..." aria-label="Search" style="background-color: #f8f9fa; width: 200px;">
            <button class="btn btn-sm border-0 rounded-pill px-1" type="submit" style="color: #fe424d;">
              <i class="fa-solid fa-search"></i>
            </button>
          </form>
        </li>
      </ul>
    </div>

    <button class="navbar-toggler border-0 ms-1" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  </div>
</nav>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const searchContainer = document.querySelector('.search-container');
    const searchToggle = document.querySelector('.search-toggle');
    const searchInput = document.querySelector('.search-input');

    if (searchToggle && searchInput && searchContainer) {
        // Submit search form on search button click only if input has value
        searchToggle.addEventListener('click', function() {
            if (searchInput.value.trim() !== '') {
                searchContainer.submit();
            }
        });

        // Submit form when Enter is pressed in search input only if input has value
        searchInput.addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                e.preventDefault();
                if (searchInput.value.trim() !== '') {
                    searchContainer.submit();
                }
            }
        });
    }

    // Handle mobile search dropdown
    const mobileSearchForm = document.querySelector('#searchDropdown form');
    const mobileSearchInput = mobileSearchForm ? mobileSearchForm.querySelector('input[name="search"]') : null;
    const mobileSearchButton = mobileSearchForm ? mobileSearchForm.querySelector('button') : null;

    if (mobileSearchButton && mobileSearchInput) {
        mobileSearchButton.addEventListener('click', function(e) {
            if (mobileSearchInput.value.trim() === '') {
                e.preventDefault();
            }
        });

        mobileSearchInput.addEventListener('keypress', function(e) {
            if (e.key === 'Enter') {
                e.preventDefault();
                if (mobileSearchInput.value.trim() !== '') {
                    mobileSearchForm.submit();
                }
            }
        });
    }
});
</script>